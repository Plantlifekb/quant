version: "3.9"

services:
  dashboard:
    image: quant-dashboard
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8050:8050"
    volumes:
      - C:\Quant\data:/data
    restart: unless-stopped