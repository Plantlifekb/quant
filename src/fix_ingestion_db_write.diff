*** Begin Patch
*** Update File: quant/engine/tasks/ingestion.py
@@
 def write_prices_to_db(df: pd.DataFrame) -> int:
     """
     Write normalized prices to Postgres. Returns number of rows written.
     """
-    pg_dsn = os.getenv("PG_DSN", "")
-    conn = psycopg2.connect(pg_dsn)
+    pg_dsn = os.getenv("PG_DSN", "")
+    # Try to use PG_DSN directly; if it's not a valid psycopg2 DSN (e.g. a sqlite URL),
+    # fall back to building a connection from individual PG environment variables.
+    try:
+        if pg_dsn:
+            conn = psycopg2.connect(pg_dsn)
+        else:
+            # No PG_DSN provided; build from env vars
+            conn = psycopg2.connect(
+                host=os.getenv("PGHOST", "localhost"),
+                port=os.getenv("PGPORT", "5432"),
+                dbname=os.getenv("PGDATABASE", "postgres"),
+                user=os.getenv("PGUSER", "postgres"),
+                password=os.getenv("PGPASSWORD", ""),
+            )
+    except Exception:
+        # Likely an invalid DSN for psycopg2 (for example a sqlite:// URL).
+        # Build a proper psycopg2 connection from the standard PG env vars and retry.
+        conn = psycopg2.connect(
+            host=os.getenv("PGHOST", "localhost"),
+            port=os.getenv("PGPORT", "5432"),
+            dbname=os.getenv("PGDATABASE", "postgres"),
+            user=os.getenv("PGUSER", "postgres"),
+            password=os.getenv("PGPASSWORD", ""),
+        )
*** End Patch